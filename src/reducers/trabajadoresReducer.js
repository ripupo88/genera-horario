import { types } from "./types";

export const trabajadoresReducer = (state, action) => {
    switch (action.type) {
        case types.generaHorario:
            // Aqui la logica
            break;

        default:
            break;
    }
    return state;
};
